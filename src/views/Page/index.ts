/**
 * Page Component
 * Router for all paths, maintains top nav and renders
 * children components based on route.
 * Login route overrides others as a special case.
 */
export { default } from './Page';
